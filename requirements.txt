# ── Core API framework ──────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
httpx==0.27.0
pydantic==2.7.1

# ── ML / Vision ─────────────────────────────────────────────────────────────────
# NOTE: torch/torchvision come from the CUDA wheel index, NOT PyPI —
# the "+cu121" local-version builds do not exist on PyPI. Install with:
#   pip install -r requirements.txt  (honours the --index-url line below)
--index-url https://download.pytorch.org/whl/cu121
torch==2.3.0+cu121
torchvision==0.18.0+cu121
Pillow==10.3.0
numpy==1.26.4
opencv-python-headless==4.9.0.80
safetensors==0.7.0

# ── Audio ───────────────────────────────────────────────────────────────────────
librosa==0.10.2
soundfile==0.12.1

# ── Transformers (for UniversalFakeDetect / ViT models) ─────────────────────────
transformers==4.41.0
timm==1.0.3

# ── Utilities ───────────────────────────────────────────────────────────────────
scipy==1.13.0
tqdm==4.66.4
